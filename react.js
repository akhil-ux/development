import react from 'react';
import ReactDOM from 'react-dom/client';
var myelement = <h1>Hello World,Welcome to React world!!!</h1>
const root = ReactDom.createRoot(document.getElementById('root'));
root.render(myelement);